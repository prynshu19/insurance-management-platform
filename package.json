{
  "name": "insurance-management-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "npm --prefix server install && npm --prefix client install --include=dev",
    "build": "npm --prefix client install --include=dev && npm --prefix client run build && npm --prefix server install && npm --prefix server run build",
    "start": "npm --prefix server start",
    "dev": "npm --prefix server run dev",
    "install:all": "npm --prefix server install && npm --prefix client install --include=dev",
    "build:client": "npm --prefix client install --include=dev && npm --prefix client run build",
    "build:server": "npm --prefix server install && npm --prefix server run build",
    "start:server": "npm --prefix server start"
  }
}
